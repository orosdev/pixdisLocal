import {colors} from "../../theme/colors";

export const styles = {
    buttonContainer: {
        width: '100%',
        borderRadius: 23,
        height: 46,
        border: "none",
        backgroundColor: colors.secondary,
        cursor: "pointer"
    },
    text: {
        color: colors.primary
    }
}
